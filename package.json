{
  "name": "opencollab-lib",
  "version": "0.0.1",
  "description": "Library port of OpenCollab",
  "main": "public/index.js",
  "scripts": {
    "test": "mocha",
    "build": "rm -rf public && mkdir public && cp -r build public/build && babel . --out-dir public --ignore node_modules",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/macor161/opencollab-lib.git"
  },
  "keywords": [
    "opencollab",
    "mango",
    "git"
  ],
  "bugs": {
    "url": "https://github.com/macor161/opencollab-lib/issues"
  },
  "dependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.3.3",
    "babel-register": "^6.24.1",
    "fs-extra": "^5.0.0",
    "mkdirp": "^0.5.1",
    "shelljs": "^0.7.7",
    "swarm-js": "^0.1.10",
    "truffle-contract": "^2.0.0",
    "web3": "^0.16.0",
    "yargs": "^4.7.1"
  },
  "yargs": {
    "parse-numbers": false
  },
  "devDependencies": {
    "assert": "^1.4.1"
  }
}
